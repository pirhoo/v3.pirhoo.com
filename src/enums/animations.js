export const Duration = Object.freeze({
  FAST: 150,
  NORMAL: 300,
  OFFCANVAS_CLOSE: 300,
  COUNT_ANIMATION: 2000
})

export const Threshold = Object.freeze({
  VISIBILITY: 0.1,
  CONTRAST_WCAG_AA: 4.5,
  SWIPE: 30,
  DRAG: 3
})
